diff --git a/VoiceInk/Info.plist b/VoiceInk/Info.plist
index 4d98a62..448c9e8 100644
--- a/VoiceInk/Info.plist
+++ b/VoiceInk/Info.plist
@@ -9,13 +9,13 @@
 	<key>SUPublicEDKey</key>
 	<string>gnXep9+mzNXFSbX0jU7CpLtubcQ5WVfVGe0BxsSdS40=</string>
 	<key>LSUIElement</key>
-	<false/>
+	<true/>
 	<key>SUEnableAutomaticChecks</key>
 	<false/>
 	<key>SUScheduledCheckInterval</key>
 	<integer>14400</integer>
 	<key>NSMicrophoneUsageDescription</key>
-	<string>VoiceInk needs access to your microphone to record audio for transcription.</string>
+	<string>Syll needs access to your microphone to record audio for transcription.</string>
 	<key>NSAppleEventsUsageDescription</key>
 	<string>VoiceInk needs to interact with your browser to detect the current website for applying website-specific configurations.</string>
 	<key>NSScreenCaptureUsageDescription</key>
diff --git a/VoiceInk/Services/KeychainService.swift b/VoiceInk/Services/KeychainService.swift
index 3ec45c0..bf06596 100644
--- a/VoiceInk/Services/KeychainService.swift
+++ b/VoiceInk/Services/KeychainService.swift
@@ -23,9 +23,11 @@ final class KeychainService {
         case unavailable(OSStatus)
     }
 
-    private let logger = Logger(subsystem: "com.prakashjoshipax.voiceink", category: "KeychainService")
+    private let logger = Logger(subsystem: "capital.underbite.syll", category: "KeychainService")
     #if LOCAL_BUILD
-        private let service = "com.prakashjoshipax.VoiceInk.Local"
+        // Never query the legacy VoiceInk service implicitly. Its item ACL belongs
+        // to the former app identity and prompts during an otherwise silent launch.
+        private let service = "capital.underbite.syll.Local"
         private let defaults = UserDefaults.standard
         private let legacyLocalPrefix = "LocalKeychain_"
     #else
diff --git a/VoiceInk/Services/LaunchAtLoginManager.swift b/VoiceInk/Services/LaunchAtLoginManager.swift
index 580e641..8c3ae18 100644
--- a/VoiceInk/Services/LaunchAtLoginManager.swift
+++ b/VoiceInk/Services/LaunchAtLoginManager.swift
@@ -10,7 +10,8 @@ final class LaunchAtLoginManager: ObservableObject {
     @Published private(set) var isEnabled = false
     @Published private(set) var isUpdating = false
 
-    private let logger = Logger(subsystem: "com.prakashjoshipax.voiceink", category: "LaunchAtLogin")
+    private let logger = Logger(subsystem: "capital.underbite.syll", category: "LaunchAtLogin")
+    private let duplicateRegistrationCleanupKey = "SyllRemovedDuplicateServiceLoginItem"
     private var isRefreshing = false
     private var operationGeneration = 0
     private var pendingEnabledState: Bool?
@@ -18,6 +19,12 @@ final class LaunchAtLoginManager: ObservableObject {
 
     private init() {
         refresh()
+        if UserDefaults.standard.bool(forKey: "SyllDidRequestInitialLaunchAtLogin")
+            && !UserDefaults.standard.bool(forKey: duplicateRegistrationCleanupKey)
+        {
+            UserDefaults.standard.set(true, forKey: duplicateRegistrationCleanupKey)
+            setEnabled(false)
+        }
     }
 
     func refresh() {
diff --git a/VoiceInk/Shortcuts/ShortcutMonitor.swift b/VoiceInk/Shortcuts/ShortcutMonitor.swift
index 8647060..6365b9f 100644
--- a/VoiceInk/Shortcuts/ShortcutMonitor.swift
+++ b/VoiceInk/Shortcuts/ShortcutMonitor.swift
@@ -92,15 +92,15 @@ final class ShortcutMonitor {
                 return Unmanaged.passUnretained(event)
             }
 
-            let shouldSuppress = monitor.handleCGEvent(type: type, event: event)
-            return shouldSuppress ? nil : Unmanaged.passUnretained(event)
+            _ = monitor.handleCGEvent(type: type, event: event)
+            return Unmanaged.passUnretained(event)
         }
 
         guard
             let eventTap = CGEvent.tapCreate(
                 tap: .cgSessionEventTap,
                 place: .headInsertEventTap,
-                options: .defaultTap,
+                options: .listenOnly,
                 eventsOfInterest: Self.eventMask,
                 callback: callback,
                 userInfo: Unmanaged.passUnretained(self).toOpaque()
diff --git a/VoiceInk/VoiceInk.swift b/VoiceInk/VoiceInk.swift
index 5769b2d..f238d98 100644
--- a/VoiceInk/VoiceInk.swift
+++ b/VoiceInk/VoiceInk.swift
@@ -42,6 +42,7 @@ struct VoiceInkApp: App {
         URLCache.shared = URLCache(memoryCapacity: 0, diskCapacity: 0)
 
         AppDefaults.registerDefaults()
+        SyllIdentityMigration.runIfNeeded()
         AppLanguagePreference.applyStored()
         AppAppearancePreference.applyStored()
         OnboardingV2Migration.prepareIfNeeded()
@@ -282,7 +283,7 @@ struct VoiceInkApp: App {
         Window("Syll", id: AppWindowID.main) {
             Group {
                 if hasCompletedOnboardingV2 {
-                    ContentView()
+                    SyllControlView()
                         .environmentObject(engine)
                         .environmentObject(whisperModelManager)
                         .environmentObject(fluidAudioModelManager)
@@ -300,10 +301,6 @@ struct VoiceInkApp: App {
                                 AnnouncementsService.shared.start()
                             }
 
-                            showLaunchRemindersIfNeeded()
-
-                            GitHubStarPromptCoordinator.shared.scheduleIfNeeded(modelContainer: container)
-
                             // Run due audio-only cleanup and schedule future checks when transcript cleanup is not managing retention.
                             if !UserDefaults.standard.bool(forKey: CleanupSettingsKeys.isTranscriptionCleanupEnabled)
                                 && UserDefaults.standard.bool(forKey: CleanupSettingsKeys.isAudioCleanupEnabled)
@@ -340,7 +337,7 @@ struct VoiceInkApp: App {
                             audioCleanupManager.stopAutomaticCleanup()
                         }
                 } else {
-                    OnboardingView(hasCompletedOnboardingV2: $hasCompletedOnboardingV2)
+                    SyllControlView()
                         .environmentObject(fluidAudioModelManager)
                         .environmentObject(transcriptionModelManager)
                         .environmentObject(aiService)
@@ -362,7 +359,7 @@ struct VoiceInkApp: App {
             }
         }
         .windowStyle(.hiddenTitleBar)
-        .defaultSize(width: AppWindowLayout.width, height: AppWindowLayout.minimumHeight)
+        .defaultSize(width: 720, height: 560)
         .windowResizability(.contentSize)
         .commands {
             CommandGroup(replacing: .newItem) {}
@@ -373,7 +370,7 @@ struct VoiceInkApp: App {
         }
 
         MenuBarExtra(isInserted: $showMenuBarIcon) {
-            MenuBarView()
+            SyllMenuBarView()
                 .environmentObject(engine)
                 .environmentObject(whisperModelManager)
                 .environmentObject(fluidAudioModelManager)
diff --git a/VoiceInk/VoiceInk.swift b/VoiceInk/VoiceInk.swift
--- a/VoiceInk/VoiceInk.swift
+++ b/VoiceInk/VoiceInk.swift
@@ -430,14 +430,24 @@ struct VoiceInkApp: App {
 }
 
 private struct SyllMenuBarMark: View {
+    private static let templateImage: NSImage = {
+        let image = NSImage(size: NSSize(width: 18, height: 18))
+        image.lockFocus()
+        NSColor.black.setFill()
+        let heights: [CGFloat] = [6, 9, 12, 16]
+        for (index, height) in heights.enumerated() {
+            let rect = NSRect(x: CGFloat(index) * 4 + 1, y: 1, width: 2.5, height: height)
+            NSBezierPath(roundedRect: rect, xRadius: 1, yRadius: 1).fill()
+        }
+        image.unlockFocus()
+        image.isTemplate = true
+        return image
+    }()
+
     var body: some View {
-        HStack(alignment: .bottom, spacing: 1.5) {
-            RoundedRectangle(cornerRadius: 1).frame(width: 2.5, height: 7)
-            RoundedRectangle(cornerRadius: 1).frame(width: 2.5, height: 11)
-            RoundedRectangle(cornerRadius: 1).frame(width: 2.5, height: 15)
-            RoundedRectangle(cornerRadius: 1).frame(width: 2.5, height: 19)
-        }
-        .frame(width: 16, height: 22, alignment: .bottom)
+        Image(nsImage: Self.templateImage)
+            .renderingMode(.template)
+            .frame(width: 18, height: 18)
         .accessibilityLabel("Syll")
     }
 }
diff --git a/VoiceInk/WindowManager.swift b/VoiceInk/WindowManager.swift
index d97f793..54f47aa 100644
--- a/VoiceInk/WindowManager.swift
+++ b/VoiceInk/WindowManager.swift
@@ -2,8 +2,8 @@ import AppKit
 import SwiftUI
 
 enum AppWindowLayout {
-    static let width: CGFloat = 950
-    static let minimumHeight: CGFloat = 750
+    static let width: CGFloat = 720
+    static let minimumHeight: CGFloat = 520
 }
 
 enum AppWindowID {
