diff --git a/VoiceInk/Services/AIEnhancement/AIEnhancementService.swift b/VoiceInk/Services/AIEnhancement/AIEnhancementService.swift
--- a/VoiceInk/Services/AIEnhancement/AIEnhancementService.swift
+++ b/VoiceInk/Services/AIEnhancement/AIEnhancementService.swift
@@ -29,7 +29,7 @@ class AIEnhancementService: ObservableObject {
     private let customVocabularyService: CustomVocabularyService
     private var baseTimeout: TimeInterval {
         let stored = UserDefaults.standard.integer(forKey: "EnhancementTimeoutSeconds")
-        return stored > 0 ? TimeInterval(stored) : 7
+        return stored > 0 ? TimeInterval(stored) : 5
     }
     private let rateLimitInterval: TimeInterval = 1.0
     private var lastRequestTime: Date?
@@ -478,11 +478,22 @@ class AIEnhancementService: ObservableObject {
         let promptName = configuration.prompt?.title
 
         do {
+            let maxAttempts = CleanupOutputValidator.isCleanupPrompt(promptName) ? 1 : 3
             let requestResult = try await makeRequestWithRetry(
                 text: text,
                 configuration: configuration,
-                contextSnapshot: contextSnapshot
+                contextSnapshot: contextSnapshot,
+                maxRetries: maxAttempts
+            )
+            let validatedText = try CleanupOutputValidator.validatedText(
+                candidate: requestResult.text,
+                source: text,
+                promptTitle: promptName,
+                protectedTerms: customVocabularyService.getPreferredTerms(
+                    from: modelContext
+                )
             )
             let endTime = Date()
             let duration = endTime.timeIntervalSince(startTime)
             return AIEnhancementResult(
-                text: requestResult.text,
+                text: validatedText,
                 duration: duration,
                 promptName: promptName,
                 systemMessage: requestResult.systemMessage,
diff --git a/VoiceInk/Transcription/Engine/TranscriptionPipeline.swift b/VoiceInk/Transcription/Engine/TranscriptionPipeline.swift
--- a/VoiceInk/Transcription/Engine/TranscriptionPipeline.swift
+++ b/VoiceInk/Transcription/Engine/TranscriptionPipeline.swift
@@ -93,6 +93,7 @@ class TranscriptionPipeline {
         }
 
         do {
+            PersonalDictionaryService.refreshRecognitionCache(from: modelContext)
             let transcriptionStart = Date()
             var text: String
             if let session {
